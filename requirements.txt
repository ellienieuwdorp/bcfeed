# Runtime dependencies, pinned to the versions bcfeed is developed and tested against.
#
# pyproject.toml is the CANONICAL packaging source (its [project.dependencies]
# carries these same pins). This file is kept in sync as a convenience so the
# README's `pip install -r requirements.txt` flow keeps working; `pip install .`
# uses pyproject. Keep the two lists identical when bumping a pin (WP-28/ARCH-9).
#
# Homebrew formula note: the formula must gain keyring, markdown-it-py, and
# linkify-it-py (added with keychain storage and the docs renderer) at the next release.
google-api-python-client==2.198.0
google-auth-oauthlib==1.4.0
requests==2.34.2
beautifulsoup4==4.15.0
flask==3.1.3
furl==2.1.4
keyring==25.7.0
markdown-it-py==4.2.0
linkify-it-py==2.1.0
