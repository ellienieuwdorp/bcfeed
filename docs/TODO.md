# TODO

## To do

- Package in an executable with an installer.

## Done

- Add a "show only" tick column to the label list.
- Save metadata scraped from emails for a given period.
- Save read/unread state between sessions.
- Add a settings page:
  - Clear cache
  - Clear read/unread state
  - Clear all
- Persist server settings.
- Add a "clear cache" button.
- Date filter.
- Mark all as seen/unseen.
- Embed cache persistence.
- Highlight unseen rows in a different shade.
- Date column width.
- Enable/disable cached badge.
- Refactor out of `BandcampReleaseSummary.py` and delete it.
- Persist the cached-badge setting.
- Fix: selecting "Clear cache" in settings also cleared the read/unread state.
- Output to the same HTML file.
